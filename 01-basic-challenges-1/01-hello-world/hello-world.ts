/**
 * Function that returns the input string.
 * @param {string} [str='Hello World!'] - The input string to be returned.
 * @return {string} - The same input string.
 */
export function helloWorld(str: string = 'Hello World!'): string {
  return str;
}
